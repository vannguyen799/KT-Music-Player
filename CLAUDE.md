# KT Music Player - Development Guidelines

## Icons
- Always use inline SVG for icons. Never use text characters (Unicode symbols, HTML entities, emoji) as icons because they render inconsistently between iOS Safari and Chrome.
- Use the `Icon.svelte` component (`$lib/components/Icon.svelte`) for common icons.
- SVG resources: https://www.svgrepo.com/
